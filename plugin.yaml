name: hermes-allow
version: "1.0.0"
description: "Terminal command guard for Hermes Agent. Runs in allow mode (only listed commands pass) or block mode (only listed commands are stopped)."
author: StewLab
kind: standalone
provides_hooks:
  - pre_tool_call
