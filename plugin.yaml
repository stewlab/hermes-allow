name: hermes-allow
version: "1.1.2"
description: "Terminal command guard for Hermes Agent. Allow mode: only listed commands are allowed. Block mode: only listed commands are blocked."
author: StewLab
kind: standalone
provides_hooks:
  - pre_tool_call
